python-docx>=1.1
pyyaml>=6.0            # customer_profile.yaml parsing for the report renderer
openpyxl>=3.1
pillow>=10.0
# Optional, only if you use the Agent SDK runner instead of the Claude Code CLI:
# claude-agent-sdk
# python-dotenv
# NOTE: Azure Key Vault resolution is done by your cti-feeds launchers (their repo,
# their deps). No azure-* packages are needed by THIS repo.
