# Runtime deps for the cti-pipeline image (pip, not uv: no lockfile is shipped and
# core/pyproject.toml pins Python 3.13 which the base image does not carry).
# Doc builders and the report renderer
python-docx>=1.1
openpyxl>=3.1
pillow>=10.0
pyyaml>=6.0
# threatpipe MCP servers (stdio, launched from the generated .mcp.json)
fastmcp>=2.0.0
httpx>=0.27.0
